# EEG Mini Viewer

A tiny project that visualises sample EEG data using [MNE-Python](https://mne.tools/).

## Run it
1. Create & activate a conda env:
   ```bash
   conda create -n neuro101 python=3.11 -y
   conda activate neuro101
   python -m pip install --upgrade pip
   pip install -r requirements.txt
